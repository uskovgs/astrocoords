# Check whether an object is a sky_coord vector

Check whether an object is a sky_coord vector

## Usage

``` r
is.sky_coord(x)
```

## Arguments

- x:

  Any R object.

## Value

\`TRUE\` if \`x\` inherits from \`\<sky_coord\>\`, otherwise \`FALSE\`.
